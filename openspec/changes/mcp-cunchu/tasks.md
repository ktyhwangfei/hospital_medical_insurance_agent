## 1. 契约与数据模型

- [x] 1.1 新增 MCP 服务、能力、策略、状态、远程调用、流式事件、管理 UI 请求响应和审计元数据 Pydantic 模型。
- [x] 1.2 新增 MCP 注册服务、MCP 存储、MCP Client Gateway、MCP 管理 API 的 Protocol 契约。
- [x] 1.3 补充统一错误结构、脱敏字段、citations、uncertainties 和 audit_event 的 MCP 响应契约。

## 2. 持久化存储与缓存

- [x] 2.1 实现 MCP 内存存储，用于单元测试、本地开发和降级验证。
- [ ] 2.2 实现 PostgreSQL 事实数据存储，覆盖 MCP 服务、能力、工具 schema、资源索引、策略、审计索引、连接配置脱敏视图和状态快照。
- [ ] 2.3 实现 Redis/Valkey 能力缓存、连接健康状态、流式调用短期状态、幂等键、限流计数和分布式锁。
- [x] 2.4 增加 PostgreSQL 与 Redis/Valkey 的健康检查、启动校验、配置开关、缓存失效和降级策略。
- [x] 2.5 明确对象存储和配置中心不作为默认运行依赖，并为大文件、长期归档、跨环境配置发布或密钥托管预留扩展接口。

## 3. MCP 扩展注册服务

- [x] 3.1 实现 MCP 服务注册、编辑、启用、禁用、删除和状态查询逻辑。
- [x] 3.2 实现 MCP 能力发现结果入库、能力清单查询、能力策略配置和稳定排序。
- [x] 3.3 实现按场景、角色、权限、能力类型、风险等级和服务状态筛选 MCP 能力。
- [x] 3.4 实现高风险 MCP 能力识别、人工确认标记和不可自动执行约束。
- [x] 3.5 实现 MCP 注册、状态变更、能力筛选和策略变更审计记录。

## 4. 远程 MCP Client Gateway

- [x] 4.1 实现远程 MCP Server 连接配置、认证信息脱敏保存和连接生命周期管理。
- [x] 4.2 实现 MCP 初始化握手、协议版本校验、能力发现和连接测试。
- [ ] 4.3 实现 MCP 协议流式通信事件归一化，覆盖增量事件、完成事件和错误事件。
- [x] 4.4 实现低风险只读 MCP 工具调用、参数校验、超时控制、重试策略和结果归一化。
- [x] 4.5 实现 MCP 鉴权失败、限流、超时、断连、畸形消息和上游错误的统一异常归一化。

## 5. 运行时与安全集成

- [x] 5.1 将 MCP 能力筛选接入运行时计划或编排步骤，记录 selected_capabilities、excluded_capabilities 和选择原因。
- [ ] 5.2 将授权后的低风险远程 MCP 调用接入 workflow 状态，记录 request_id、server_id、capability_id、stream summary、latency 和 audit_event。
- [x] 5.3 将高风险 MCP 能力接入现有风控与人工确认任务闭环，确保不执行真实工具调用。
- [ ] 5.4 对 MCP 调用结果执行脱敏、引用追踪、不确定性补齐和统一响应组装。
- [ ] 5.5 增加 MCP 注册服务、存储服务或远程调用不可用时的降级路径，确保既有医保导办主流程可继续返回。

## 6. MCP 管理 API 与 UI

- [x] 6.1 新增 MCP 管理 API，覆盖服务注册、编辑、启停、删除、详情、列表和状态查询。
- [ ] 6.2 新增连接测试 API，返回握手状态、协议版本、能力数量、错误详情和审计事件。
- [ ] 6.3 新增能力浏览与策略配置 API，覆盖工具、资源、提示、服务能力和风险权限策略。
- [ ] 6.4 新增 MCP 审计查询 API，支持按 server_id、capability_id、workflow_id、audit_event 和时间范围查询。
- [x] 6.5 新增 MCP 管理 UI 页面，支持服务管理、连接测试、能力浏览、策略配置、审计查看和存储状态查看。
- [x] 6.6 确保 UI 对密钥、Token、连接串和患者敏感字段仅展示脱敏值。

## 7. 测试与验证

- [x] 7.1 新增 MCP 模型、注册服务、能力筛选和策略配置单元测试。
- [x] 7.2 新增 MCP 内存存储、PostgreSQL 事实数据存储、Redis/Valkey 缓存与短期状态测试。
- [x] 7.3 新增 MCP 远程连接握手、能力发现、流式通信、工具调用和错误归一化测试。
- [x] 7.4 新增 MCP 高风险拦截、权限拒绝、脱敏和审计安全边界测试。
- [x] 7.5 新增运行时集成测试，覆盖低风险 MCP 调用成功、高风险转人工确认和 MCP 不可用降级。
- [x] 7.6 新增 MCP 管理 API 与 UI 的端到端或契约测试。
- [x] 7.7 运行 `npx openspec validate "mcp-cunchu" --strict` 和 `python -m pytest src/tests -v` 完成验证。
