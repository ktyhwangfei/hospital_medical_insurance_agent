请按 #33 §8.6 先做加固点 ①：structured 空上下文必须拒答。

背景/根因（来自终态报告 §8.4/§8.6）：structured 检索 84? 的 6 条负例误答，是 eval 把空上下文 BROAD_* 问题喂给 structured 口径产物（生产路由不会如此），但暴露了"空 ctx 应拒答"的加固点。目标就是：无论来自 eval 还是任何调用，一旦 structured 在无有效上下文（无命中/无有效语料段）时，不得用兜底泛答案，必须走诚实拒答出口（与既有诚实拒答通道一致），返回"无依据/无法回答"类结果，禁止编造。

验收标准：
1. 对 empty/无效上下文 的请求 → 触发诚实拒答，message 明确表达"缺少可依据的政策上下文"。给出可复现用例（含把那 6 条 BROAD 空 ctx 负例走 structured 的行为断言）。
2. 不破坏现有 82 条语料正例（structured true-positive 命中路径 PR/召回不回退，用同基线报告 bb2ea67 口径对比）。
3. 不引入死代码/过度抽象；改动尽量小，落在读路径 resolver/检索出口处统一处理，而非每个 caller 补 guard。
4. 跑语义检索回归（structured+broad 基线对照），把加固后的指标结果与 bb2ea67 基线并列输出差异。
5. 落档：更新 issue #33 需求迭代记录 + PROGRESS.md，提交（Angular 规范，前缀 test/feat(knowledge) 视改动），commit message 带【加固①空ctx拒答】。

完成后回报：改了哪些文件、单测/回归数字、与 bb2ea67 基线 diff 摘要。门禁口令不变：三项全过(诚实拒答率>80% / FAR<8% / P@3>90%)才复测放行，本次加固是否先触达标只看数据说话。
