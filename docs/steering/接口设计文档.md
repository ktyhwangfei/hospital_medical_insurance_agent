# 接口设计文档

**版本**: 2.1 | **日期**: 2026-06-19

---

## 1. 业务场景总览

```
                         Portal (3000) / Admin (3001)
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
        /policy-qa       /policy-knowledge   管理接口
    (政策问答+费用解释)   (Milvus 规则管理)  (MCP/模型/Skill)
              │               │
              ▼               ▼
          SkillRouter    policy_knowledge_routes
              │               │
              ▼               ▼
    StructuredPolicyRuleRetriever (Milvus)
              │
              ▼
    SQL Server (结算数据) + Milvus (政策规则)
```

| 场景 | 入口接口 | 说明 |
|------|----------|------|
| 政策问答（费用解释） | `GET /policy-qa/settlement-explanation` | Skill 驱动，Strategy Pattern 处理统筹自付/起付线/大额自付 |
| 政策问答（流式） | `POST /policy-qa/stream` | SSE 流式输出 |
| 政策问答历史 | `GET /policy-qa/history` | 问答历史记录（session + workflow + task） |
| 政策知识管理 | `GET /policy-knowledge/rules` | Milvus policy_rules 集合 CRUD |
| MCP 管理 | `/mcp/*` | MCP 服务器注册与能力管理 |
| 模型管理 | `/model-*` | 模型配置、路由（含退避链/模型参数）、Provider |
| Skill 基础设施 | `/infra-skills/*` | Skill 加载与测试 |

---

## 2. 接口清单

所有接口前缀 `/api/v1/medical-insurance-ai-agent`（除 `/health` 外）。

| 方法 | 路径 | 说明 |
|------|------|------|
| **政策问答** | | |
| `POST` | `/policy-qa/stream` | 政策问答 SSE 流式 |
| `POST` | `/policy-qa/test` | 政策问答测试（非流式） |
| `GET` | `/policy-qa/settlement-explanation` | 结算费用解释（Skill 驱动 + 结构化政策检索） |
| `GET` | `/policy-qa/history` | 问答历史记录（分页） |
| `GET` | `/policy-qa/debug/structured-policy-search` | 调试端点：结构化政策检索全过程 |
| **政策知识管理** | | |
| `GET` | `/policy-knowledge/rules` | 政策规则列表（分页+搜索+过滤） |
| `POST` | `/policy-knowledge/rules/query` | 高级表达式查询 |
| `GET` | `/policy-knowledge/rules/{rule_id}` | 规则详情 |
| `PUT` | `/policy-knowledge/rules/{rule_id}` | 编辑规则 |
| `DELETE` | `/policy-knowledge/rules/{rule_id}` | 删除规则 |
| `GET` | `/policy-knowledge/stats` | 集合统计 |
| `POST` | `/policy-knowledge/import` | 触发 Excel 数据导入（返回命令行指引） |
| **MCP 管理** | | |
| `GET` | `/mcp/servers` | MCP服务器列表 |
| `POST` | `/mcp/servers` | 注册MCP服务器 |
| `GET` | `/mcp/servers/{server_id}` | MCP服务器详情 |
| `GET` | `/mcp/capabilities` | MCP能力列表 |
| `POST` | `/mcp/capabilities` | 注册MCP能力 |
| `GET` | `/mcp/capabilities/{capability_id}` | MCP能力详情 |
| `DELETE` | `/mcp/capabilities/{capability_id}` | 删除MCP能力 |
| `GET` | `/mcp/capabilities/by-server/{server_id}` | 按服务器列出MCP能力 |
| `GET` | `/mcp/storage/health` | MCP存储健康检查 |
| **模型管理** | | |
| `GET` | `/model-config` | 模型配置 |
| `PUT` | `/model-config` | 更新模型配置（含 api_key） |
| `GET` | `/model-routes` | 模型路由列表 |
| `POST` | `/model-routes` | 新增路由 |
| `GET` | `/model-routes/fallbacks/{model_name}` | 查询退避链 |
| `PUT` | `/model-routes/fallbacks/{model_name}` | 更新退避链 |
| `GET` | `/model-routes/params/{model_name}` | 查询模型参数 |
| `PUT` | `/model-routes/params/{model_name}` | 更新模型参数 |
| `GET` | `/model-routes/{route_id}` | 路由详情 |
| `PUT` | `/model-routes/{route_id}` | 更新路由 |
| `DELETE` | `/model-routes/{route_id}` | 删除路由 |
| `GET` | `/model-providers` | Provider列表 |
| `POST` | `/model-providers` | 注册Provider |
| `GET` | `/model-providers/{provider_id}` | Provider详情 |
| `PUT` | `/model-providers/{provider_id}` | 更新Provider |
| `DELETE` | `/model-providers/{provider_id}` | 移除Provider |
| `POST` | `/model-providers/{provider_id}/test` | Provider 连通性测试 |
| **Skill 基础设施** | | |
| `GET` | `/infra-skills` | 已加载 Skill 列表 |
| `GET` | `/infra-skills/{skill_id}` | Skill 详情（含 README、文件结构） |
| `POST` | `/infra-skills/route-test` | 路由测试 |
| `POST` | `/infra-skills/{skill_id}/test` | Skill 执行测试 |

---

## 3. 政策问答 (Policy QA / Skill 驱动)

政策问答面向患者和收费员提供费用解释服务。架构采用 Skill 驱动 + Strategy Pattern。

### 3.1 架构概览

用户问题 → SkillRouter → policy-fee-explanation Skill → Strategy (pooling_self_pay/deductible/large_amount) → StructuredPolicyRuleRetriever (Milvus) → SkillResult

### 3.2 GET /policy-qa/settlement-explanation

费用解释主接口。路径: GET /api/v1/medical-insurance-ai-agent/policy-qa/settlement-explanation

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| settlement_id | string | 是 | 结算单号 |
| question | string | 否 | 用户问题 |

费用项路由 (SkillRouter -> target_fee_item):

| 触发关键词 | target_fee_item | Strategy |
|-----------|----------------|----------|
| 统筹自付 | pooling_self_pay | PoolingSelfPayStrategy |
| 起付线 | deductible | DeductibleStrategy |
| 大额自付 | large_amount_self_pay | LargeAmountStrategy |

响应: { target_fee_item, target_amount, definition, case_context, policy_evidence, patient_answer, office_answer, explanation_completeness, warnings }

### 3.3 POST /policy-qa/stream

SSE 流式端点。事件类型: step / trace_event / result / done。请求体 `PolicyQARequest`（必填 question + settlement_id）。流内依次经过 settlement 查询→政策规则检索→费用拆分→LLM 解释生成。

### 3.4 POST /policy-qa/test

非流式测试端点（返回请求回显），用于快速验证端点可用性。

### 3.5 GET /policy-qa/history

问答历史记录，支持 user_id 过滤 + 分页（limit/offset）。返回全量 session、workflow、task 数据。

### 3.6 GET /policy-qa/debug/structured-policy-search

调试端点，展示结构化政策规则检索完整过程：settlement_context → normalized_policy_context → planned_queries → structured_hits → selected_policy_evidence → missing_required_rules → dedupe_info。

### 3.7 政策知识 CRUD（/policy-knowledge）

Milvus policy_rules 集合 CRUD。支持分页 + keyword + 多字段过滤（rule_type, insu_type, hosp_lv, psn_type, med_type, setl_type, admission_order, amount_band, priority）。

| 端点 | 说明 |
|------|------|
| `GET /rules` | 分页查询（page, page_size, keyword, 9 个过滤字段） |
| `POST /rules/query` | 高级表达式查询（自定义 expr, limit, offset, output_fields） |
| `GET /rules/{rule_id}` | 单条规则详情 |
| `PUT /rules/{rule_id}` | 更新规则（先删再插，仅可编辑字段） |
| `DELETE /rules/{rule_id}` | 删除规则 |
| `GET /stats` | 集合统计（总量 + rule_type/insu_type/hosp_lv/psn_type/med_type 分布） |
| `POST /import` | 触发数据导入（当前未实现，返回命令行指引） |

### 3.8 Skill 架构

SkillLoader: 扫描 skills/ 动态发现。SkillRouter: 关键词路由。新增 Skill: 创建目录 + manifest + assembler 即自动发现。

### 3.9 Strategy Pattern

strategies/ 下每个费用项独立策略，各自拥有 definition / policy_queries / patient_template / office_template / calculation_trace / warnings / completeness。

---

## 4. MCP 管理

所有端点见 §2 接口清单。路由前缀 `/api/v1/medical-insurance-ai-agent/mcp`。

| 分组 | 端点 | 说明 |
|------|------|------|
| Servers | `GET /servers, POST /servers, GET /servers/{id}` | MCP服务器注册与查询 |
| Capabilities | `GET /capabilities, POST /capabilities, GET /capabilities/{id}, DELETE /capabilities/{id}` | MCP能力管理 |
| Capabilities | `GET /capabilities/by-server/{server_id}` | 按服务器筛选能力列表 |
| Health | `GET /storage/health` | 存储健康检查 |

MCP 注册中心使用 `McpRegistryService`，注册时执行安全校验（风险等级 + 角色权限）。

---

## 5. 模型管理

所有端点见 §2 接口清单。路由前缀 `/api/v1/medical-insurance-ai-agent`，共 17 个端点。

| 分组 | 端点 | 说明 |
|------|------|------|
| 配置 | `GET/PUT /model-config` | 模型全局配置（base_url, timeout, default_model, api_key） |
| 路由 | `GET /model-routes, POST /model-routes` | 路由规则列表与新增 |
| 路由 | `GET/PUT/DELETE /model-routes/{route_id}` | 路由规则 CRUD |
| 退避链 | `GET/PUT /model-routes/fallbacks/{model_name}` | 模型降级退避链管理 |
| 参数 | `GET/PUT /model-routes/params/{model_name}` | 模型参数管理（temperature, max_tokens, top_p） |
| Provider | `GET /model-providers, POST /model-providers` | Provider 列表与注册 |
| Provider | `GET/PUT/DELETE /model-providers/{provider_id}` | Provider CRUD |
| Provider | `POST /model-providers/{provider_id}/test` | Provider 连通性测试（HTTP GET 基地址） |

当前使用模块级 `dict` 作为运行时存储（未接入持久化）。api_key 在响应中脱敏（首尾各保留 4 位）。

---

## 6. Skill 基础设施

见接口清单 §2。端点: `/infra-skills`, `/infra-skills/{id}`, `/infra-skills/route-test`, `/infra-skills/{id}/test`

### 6.1 GET /infra-skills

返回所有已加载 Skill 列表。响应体：

```json
{
  "skills": [
    {
      "skill_id": "settlement_explain_skill",
      "skill_name": "医保费用解释",
      "business_action": "explain",
      "business_object": "settlement",
      "include_keywords": ["统筹自付", "起付线", "报销比例", ...],
      "excluded_intents": []
    }
  ]
}
```

`business_action` 为 `BusinessAction` 七类枚举之一（explain / query / guide / verify / compare / evaluate / analyze），
`business_object` 为 `BusinessObject` 枚举值（settlement / benefit / policy / directory / chronic_disease / referral / appeal / medical_record / drg_dip / complaint）。
两者共同唯一确定一个 Skill。

### 6.2 路由机制

用户问题 → Business Action Recognition（做什么）→ Business Object Recognition（处理谁）→ Skill Router（关键词 + LLM 消歧）→ Skill Execution
