---
context: default
phase: skill-ai-authoring
task: 4
total_tasks: 9
status: paused
last_updated: 2026-08-10T06:24:56.064Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: 共享工作区提交污染 — 本次已实际发生其他代理的 commit 误带 Task 1 新建文件 — 继续前必须先运行 `git status --short`，提交时显式按文件/分块暂存，并在 commit 前审查 `git diff --cached --name-status` 与 `git diff --cached`。
- [ ] CONSTRAINT: 外部 regression 改动必须保留 — 当前工作树有 governance/regression evaluator 相关未提交文件，它们不属于 Task 4 — 不得修改、删除、暂存或提交。
- [ ] CONSTRAINT: Task 4 尚未进入 RED — 子代理仅做了只读调查 — 恢复后的第一个代码动作必须是写 API 失败测试并观察 404/403/409 的预期 RED，不得先写生产代码。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| 宽泛 `git add` | 共享工作区中使用宽泛暂存，曾使 Task 1 测试/实现被带入 policy QA 与 regression 提交 | blocking | 仅显式 `git add <owned-files>` 或精确分块；commit 前检查 cached diff |
| route 全局 evidence dict | Task 4 若用进程内 route 全局 dict，无 TTL、不可注入、多进程失效 | blocking | 定义窄 evidence store 边界，适配现有 `ShortStateStore`/cache，测试注入内存 fake |
| 用 prompt 代替服务端门禁 | Task 2/3 审查实际发现 AST 别名、注解、PEP 695、非法 action/object 等多个绕过 | advisory | 业务矩阵、文件、敏感信息、hash 和 snapshot 必须由服务端重新校验 |

<current_state>

当前正执行 `docs/superpowers/plans/2026-08-10-skill-ai-authoring.md`，位于 Task 4/9。Task 1–3 已完成并通过规格审查、代码质量审查和新鲜回归。Task 4 子代理在写任何代码前被停止：只完成现有 API、cache、draft service 与 authoring service 调查，未新增测试，未运行 RED，未修改 Task 4 文件。

当前共享 HEAD 为 `429503d` (`feat: typed case proposal editor + eval-mining workflow UI`)；Task 3 最终提交为 `983b803` (`fix: harden skill ai proposal integrity`)。Task 4 开工前 API 基线 `src/tests/integration/api/test_skill_draft_api.py` 为 15 passed。

</current_state>

<completed_work>

Completed Tasks:

- Task 1: 领域契约与跨层 DTO — Done。主提交 `7cbaa6f`，加固 `0ba8014`/`9de3920`；最新联合验证 41 passed，最终质量审查 Ready: Yes。
- Task 2: 文件/AST/敏感信息/大小门禁 — Done。提交 `161318f`/`4f4d3c5`/`95bb651`/`d7956f0`/`93302c0`；最新验证 72 passed，最终质量审查 Ready: Yes。
- Task 3: ModelGateway 可追溯 proposal — Done。提交 `536c13e`/`93aafb6`/`c49b3ee`/`983b803`；独立核心回归 197 passed，审查者扩展回归 326 passed，最终质量审查 Ready: Yes。

Task 3 已实现：不可变 published metric snapshot、最多一次 repair、完整 proposal 敏感信息 fail-closed、strict JSON/hash/provenance、action/object 矩阵、640 KiB outer response 门禁、Gateway authoring hash-only audit/error 和真实 routed model 回填。

</completed_work>

<remaining_work>

- Task 4: 实现 generate/accept API、proposal evidence store、原子 `create_from_ai`、权限/幂等/篡改防护；尚未写测试或生产代码。
- Task 5: 新建向导 AI 生成与手工降级 — Not started。
- Task 6: revision 优化 diff — Not started。
- Task 7: 编辑页 diff 审查/接受 — Not started。
- Task 8: 候选制品与隔离评测 — Not started。
- Task 9: Flow/E2E/指标/PROGRESS 收口 — Not started。

</remaining_work>

<decisions_made>

- Proposal evidence 使用现有 `ShortStateStore`/cache 适配，设 TTL，测试注入内存 fake；禁止 route 全局 dict。
- Evidence state 可保存完整 proposal 供 accept 比对；audit/log 只记录 ID/hash/model/prompt version/metric refs，不记 description/script。
- 复用现有幂等 cache 的 reserve/get/complete/conflict 语义，通过最小泛型化支持 `SkillDraftResponse`，不另造一套。
- Task 4 的 DI 边界顺带消除 Task 3 对 `_input_service._registry` 的私有字段访问，改为显式窄 Protocol/registry 注入。
- `SkillPackageGenerator` 已经忽略 `__` 开头的元数据文件，Task 4 无需修改该生产文件。
- 静态新路由必须声明在动态 `/{skill_id}` 之前。
- 两个新端点都要求 `skill:release:test`；`created_by` 只取认证 principal，不信任 body actor。

</decisions_made>

<blockers>

- 无技术阻塞。Task 4 被用户换窗口主动暂停。
- 无待用户手工操作。

</blockers>

## Required Reading (in order)

1. `AGENTS.md` — 项目安全、精准修改、验证顺序与启停约束。
2. `docs/superpowers/plans/2026-08-10-skill-ai-authoring.md` — Task 4–9 权威实施计划。
3. `.planning/HANDOFF.json` 与本文件 — 当前位置、决策、未提交文件与下一动作。
4. `PROGRESS.md` — 项目全局状态；本计划完成前尚未追加 Skill AI authoring 记录。
5. `src/runtime/AGENTS.md`、`src/tests/AGENTS.md`、`src/security/AGENTS.md`、`src/domain/AGENTS.md` — Runtime/API/测试/安全/领域约束。
6. `src/runtime/skill_management/ai_authoring/service.py`、`schemas.py`、`security.py` — Task 1–3 已完成契约与门禁。
7. `src/runtime/skill_management/draft_service.py`、`src/runtime/api/infra_skill_routes.py`、`src/runtime/api/skill_schemas.py`、`src/data_platform/cache/ports.py` — Task 4 主要集成点。

## Critical Anti-Patterns (do NOT repeat these)

- 不要在生产代码之后再补 Task 4 测试：恢复后必须先观察新端点 404 RED。
- 不要在 accept 中信任客户端 provenance、metric version、hash 或 raw files：必须加载服务端 evidence，重新计算/扫描/核对。
- 不要把完整 proposal、description 或 script 写入 audit/log；Gateway 对 `skill_authoring` 已是 hash-only，Task 4 audit 也必须保持这一边界。
- 不要为 accept 复制新的幂等逻辑；复用现有 helper 语义。
- 不要修改或暂存当前外部 governance/regression evaluator 文件。

## Infrastructure State

- 本工作流没有启动后端/前端服务器，无需停止进程。
- Task 4 子代理已停止，没有后台编码任务在运行。
- 当前工作树含外部 regression 未提交改动：
  - `src/data_platform/storage/skill/governance_postgres.py`
  - `src/domain/skill/governance_models.py`
  - `src/runtime/skill_management/regression_evaluators.py`
  - `src/tests/unit/data_platform/test_skill_governance_storage.py`
  - `src/tests/unit/runtime/skill_management/test_regression_run_integration.py`

<context>

整体策略仍是 `superpowers:subagent-driven-development`：每个 Task 用新实现子代理，然后先规格审查、再质量审查，所有 Critical/Important 修复并复审后才进入下一 Task。Task 1–3 已按此执行。

Task 3 质量审查留下两个非阻塞 Minor：

1. 640 KiB model response outer envelope 对 JSON 转义最坏情况偏保守，可能误拒接近 raw file 上限的合法输出；不是安全绕过，暂不阻塞 Task 4。
2. Authoring service 仍读 `_input_service._registry`；Task 4 DI/evidence 边界中应改为显式窄 registry Protocol 注入。

</context>

<next_action>

Start with: 运行 `git status --short` 并确认外部 governance/regression 改动仍未被本任务触碰；然后仅修改 `src/tests/integration/api/test_skill_draft_api.py`，添加 generate/accept 的最小 API、权限、篡改、evidence 失效与幂等测试，使用 `app.dependency_overrides` 注入 fake gateway/registry/evidence store；运行该测试文件并确认新端点因尚未实现而返回预期 404 RED。

</next_action>
